<!--Topmenu/Topmenu.wxml-->
<view class="page-section">
    <movable-area>
      <movable-view direction="horizontal">{{userInfo.nickName}}-
      <navigator wx:for="{{TopMenuList}}" wx:key="index" url="{{item.pagePath}}">{{item.text}}-</navigator>
      </movable-view>
    </movable-area>
</view>
